// Lib Imports
import * as React from 'react';

// Local Imports


interface Props {
};

interface State {
};

// RecipeCard Component
export class RecipeCard extends React.Component<Props, State> {
	constructor(props: Props) {
		super(props);
		this.state = {};
	}
	
	render() {
		return (
			<div></div>
		);
	}
}
